// Parallax effects using Rellax

function initializeParallax() {
    // Initialize parallax
    if (document.querySelectorAll('.parallax-element').length > 0) {
        const rellax = new Rellax('.parallax-element', {
            // Configuration options
        });
    }
}